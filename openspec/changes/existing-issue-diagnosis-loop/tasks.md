## 1. Command Surface

- [x] 1.1 Add a new `./launch.sh diagnose` command.
- [x] 1.2 Add a `linear-diagnose.sh` helper.
- [x] 1.3 Persist local diagnosis bundles under a git-ignored `diagnoses/` root.

## 2. Diagnosis Flow

- [x] 2.1 Reuse the repo-backed evidence path from existing issue context.
- [x] 2.2 Support dry-run by default and `--apply` for comment/state mutation.
- [x] 2.3 Keep diagnosis conservative with deterministic fallback behavior.

## 3. Docs

- [x] 3.1 Document `diagnose` in the operator docs and docs map.
- [x] 3.2 Clarify the distinction between `intake` and `diagnose`.
