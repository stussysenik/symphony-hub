## 1. Operator Entry Surface

- [x] 1.1 Add `brief` and `resume` commands to `launch.sh`.
- [x] 1.2 Make the brief output summarize health, runtime, topology, latest checkpoint, and queue hygiene.
- [ ] 1.3 Refine the brief output after real board cleanup feedback.

## 2. OpenSpec Adoption

- [x] 2.1 Initialize OpenSpec in `symphony-hub` with Codex tooling.
- [x] 2.2 Create the `reproducible-operator-foundation` proposal, design, specs, and tasks artifacts.
- [x] 2.3 Add follow-up change proposals for Linear board normalization and review gating.

## 3. Release Management

- [x] 3.1 Add a minimal Node toolchain for semantic-release.
- [x] 3.2 Add release configuration for stable and prerelease branches.
- [x] 3.3 Add a GitHub Actions workflow for automated releases.
- [ ] 3.4 Run the first release after the repo state is fully normalized.

## 4. Documentation

- [x] 4.1 Add a docs map for the operator workflow and daily startup path.
- [x] 4.2 Update README, setup, operations, and decisions docs to reference OpenSpec, brief/resume, and semantic-release.
- [ ] 4.3 Add a follow-up progress ledger once the Linear board cleanup starts.
